# Integration Module Properties
# Placeholders will be replaced during the build

leos.user.repository.url=${leos.user.repository.url}
leos.user.repository.findbylogin.uri=/users/{userId}
leos.user.repository.searchbykey.uri=/users?searchKey={searchKey}
leos.user.repository.searchbyentitykey.uri=/entities/{entity}/users?searchKey={searchKey}

leos.export.username=${leos.export.username}
leos.export.mimetype=application/ToolboxCoDe

leos.toolBox.wsCallbackAddress=${leos.toolBox.wsCallbackAddress}
leos.toolBox.scheduler.pullingThresholdInSeconds=${leos.toolBox.scheduler.pullingThresholdInSeconds}
leos.toolBox.scheduler.cron=${leos.toolBox.scheduler.cron}
leos.toolBox.converter.jobResultPullingThresholdInSeconds=${leos.toolBox.converter.jobResultPullingThresholdInSeconds}
leos.toolBox.converter.jobResultMaxTries=${leos.toolBox.converter.jobResultMaxTries}

leos.import.oj.url=${leos.import.oj.url}
leos.import.oj.sparql.uri=${leos.import.oj.sparql.uri}

#CNS configuration
cns.notificationService.url=${cns.notificationService.url}
cns.userPreferencesService.url=${cns.userPreferencesService.url}
cns.clientSystemKey=${cns.clientSystemKey}
cns.clientSystemPassword=${cns.clientSystemPassword}
cns.notification.group=${cns.notification.group}

#Annotation configuration
annotate.api.internal.host=${annotate.server.internal.url}/api/

# Leos spell checker
leos.spell.checker.enabled=${leos.spell.checker.enabled}
leos.spell.checker.service.url=${leos.spell.checker.service.url}
leos.spell.checker.source.url=${leos.spell.checker.source.url}

leos.docuwrite.url=${leos.docuwrite.url}
leos.docuwrite.convert.uri=${leos.docuwrite.convert.uri}

leos.akn4eu.url=${leos.akn4eu.url}
leos.akn4eu.convert.uri=${leos.akn4eu.convert.uri}

leos.proxy.username=${leos.proxy.username}
leos.proxy.password=${leos.proxy.password}
leos.proxy.host=${leos.proxy.host}
leos.proxy.port=${leos.proxy.port}

pdf.generation.notification.functional.mailbox=${pdf.generation.notification.functional.mailbox}

# Leos export package
leos.export.package.notification.url=${leos.export.package.notification.url}

econsilium.search.context=${econsilium.search.context}